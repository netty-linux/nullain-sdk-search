[package]
name = "nullain-search-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure-Rust full-text local search engine (tantivy-backed) for nullain-sdk-search."

[dependencies]
tantivy = "0.22"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
